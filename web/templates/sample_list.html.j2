{% from "_sample_macros.html.j2" import sample_list %}
{% from "_macros.html.j2" import icon, filter_sort_widget, pagination %}
{% if not filteronly %}
{% extends "root.html.j2" %}
{% block title %}Samples{% endblock %}
{% block content %}
{% macro option(value, name, selected) -%}
<option value="{{ value }}" {% if selected == value %}selected{% endif %}>{{ name }}</option>
{%- endmacro %}
<h2><span class="me-2">{{ icon("box-seam") }}</span>Samples <a class="ms-2" href="{{app_prefix}}/sample/new">{{ icon("plus-square") }}</a></h2>
    <div class="mb-3">
        {{ filter_sort_widget(app_prefix ~ "/sample/list", "#sample-plus-pagination", filter_spec)}}
    </div>
    <div id="flash-messages"></div>
    <div id="sample-plus-pagination">
    <div class="text-body-tertiary">{{ summary.total_items }} items found </div>
    {{ sample_list(samples, "sample-table") }}
    {{ pagination(summary.npages, summary.page, request_uri)}}
    </div>
{% endblock %}
{% else %}
<div class="text-body-tertiary">{{ summary.total_items }} items found </div>
{{ sample_list(samples, "sample-table") }}
{{ pagination(summary.npages, summary.page, request_uri)}}
{% endif %}
